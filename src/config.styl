rolleiflex = {
	// Configurations
	"baseFontSize": 16px,		// HTML font size, for the use.em option
	"autoBorderBox": true,		// Set to false if your CSS already has something like * {box-sizing:border-box}

	// Use
	"use": {
		"em": false,		// Use the em unit instead of px (true or false),
	},
	// Builds
	"build": {
		"container": true,	    // Should build the .container CSS?
		"responsiveText": true,   	// Should build the .txt-breakpoint-position CSS?
    "verticalGrid": false     // Should build css for vertical grids?
	},
	"classScaleDivisor": "\@",
	"classPrefix": "",

	// Columns
	"columns": 12,			// Number of columns in the grid

	// Spacing
	"spaces":
	{
		"gutter": 16px,		// Gutter size
		"containerPadding":  16px		// .container--padded padding size
	},
	// Breakpoint Stuff
	"breakpoints": {
		"xs": {
			"breakpoint": 0,
		},
		"sm": {
			"breakpoint": 767px,			// Breakpoint min size = @media(min-width:xxx)
			"container": 740px				// Container max-width
		},
		"md": {
			"breakpoint": 991px,
			"container": 960px
		},
		"lg": {
			"breakpoint": 1200px,
			"container": 1170px
		}
	}
}
